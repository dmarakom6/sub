def run(command, name, options):
    # command = <to/list/cancel/...>
    # name = <str/None>
    # options = {'cost':Decimal(), 'frequency':relativedelta(), starts_on:date(), ends_on:date()/None}
    # Pseudocode:
    # subscription_list = load() #use pickle
    # run_command(command, name, options, subscription_list)
    # save(subscription_list)



def run_command(command, name, options, subscription_list):
    pass
